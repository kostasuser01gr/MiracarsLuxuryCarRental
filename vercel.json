{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack enable && corepack use pnpm@10.19.0 && pnpm install --frozen-lockfile=false && pnpm run build:web",
  "installCommand": "corepack enable && corepack use pnpm@10.19.0 && pnpm install --frozen-lockfile=false",
  "framework": null,
  "outputDirectory": "apps/web/.next"
}
